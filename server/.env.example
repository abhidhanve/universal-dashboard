# Method 3: Direct Database Connection - Server Configuration

# Server Configuration
PORT=9090
NODE_ENV=development

# Frontend Configuration  
FRONTEND_URL=http://localhost:5173

# Microservices Configuration
DB_ACCESS_SERVICE=http://127.0.0.1:9081
MOCKDATA_SERVICE=http://127.0.0.1:9083

# Default MongoDB Configuration for Method 3
MONGODB_DEFAULT_HOST=localhost
MONGODB_DEFAULT_PORT=27017
MONGODB_CONNECTION_TIMEOUT=10000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true

# Authentication Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=7d
API_KEY_PREFIX=up_test_
API_KEY_LENGTH=32

# Neon PostgreSQL Configuration (for auth)
DATABASE_URL=postgresql://username:password@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
ENABLE_AUTH=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_PREMIUM_MAX=10000

# Email Configuration (for verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
FROM_EMAIL=noreply@universalpanel.dev
FROM_NAME=Universal Panel
